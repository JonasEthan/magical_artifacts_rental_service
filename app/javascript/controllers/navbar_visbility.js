/*const nav = document.querySelector('.opacities');

function addNav(){
  nav.classList.add('show');
}
function removeNav() {
  nav.classList.remove('show');
}
var currPos = window.scrollY;
export default function toggleVisibilty() {
  console.log("Here")
  if (currPos >= window.scrollY) {
    removeNav();
  }else{
    addNav();
  }
}

document.addEventListener("scroll", () => {
  console.log("Hekk");
  setTimeout(10000, toggleVisibilty());
});
*/
