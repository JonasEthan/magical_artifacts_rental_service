/*const nav = document.querySelector('.opacities');

function addNav(){
  nav.classList.add('show');
}
function removeNav() {
  nav.classList.remove('show');
}
var currPos = window.scrollY;
export default function toggleVisibilty() {
  console.log("Here")
  if (currPos > window.scrollY) {
    nav.classList.remove('show');
  }else{
    nav.classList.add('show');
  }
}


function runIt() {
  document.addEventListener("scroll", () => {
    console.log("Hekk");
    setTimeout(5000, toggleVisibilty());
  });
}

runIt();

document.addEventListener("turbolinks:load", () =>{
  runIt();
});*/
