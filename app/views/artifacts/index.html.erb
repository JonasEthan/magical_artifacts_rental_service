<%# <%= render 'artifact_list', artifacts: @artifacts %>

<div class="container">
  <h1>Discover all artifacts (<%= @artifacts.count %>)</h1>
  <%= link_to "Create new artifact", new_artifact_path, class: "btn btn-flat" %>

  <div class="row mt-25">
    <div class="col-sm-12 col-md-6 col-lg-3">
      <% @artifacts.each do |artifact| %>
        <div class="card">
          <%= link_to artifact_path(artifact) do %>
            <img class="card-img-top" src="https://starwalk.space/gallery/images/mars-the-ultimate-guide/1920x1080.jpg" alt="Card image cap">
          <% end %>
        <div class="card-body">
          <h5 class="card-title"><%= artifact.name %></h5>
          <p class="card-text"><%= artifact.description %></p>
          <p class="card-text"><%= artifact.category %></p>
          <p class="card-text"><strong><%= artifact.price %></strong> Gold Coins / day</p>
          <a href="#" class="btn btn-flat">Book now!</a>
        </div>
    </div>
      <% end %>
  </div>


  <%= link_to "Go back", root_path, class: "btn btn-ghost" %>
</div>
